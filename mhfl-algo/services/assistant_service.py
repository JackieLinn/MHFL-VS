"""
智能助手对外入口
编排 assistant 包下的 retriever、rag、prompt、memory 等模块，供 router 调用
"""
# TODO: 步骤 6 后实现 answer()
# def answer(query: str, context_data: dict | None = None) -> tuple[str, list[str]]:
#     """非流式回答：检索 -> 拼 prompt -> 调 LLM -> 返回 content + sources"""
#     ...

# TODO: 步骤 9 后实现 answer_stream()
# async def answer_stream(query: str, context_data: dict | None = None):
#     """流式回答：逐 chunk 生成"""
#     ...
